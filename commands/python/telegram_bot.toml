description = "Scaffolds a robust Python Telegram Bot project structure using python-telegram-bot."
prompt = """
Role: Telegram Bot Architect.

1. **Ask the user**: 

Use the `ask_user` tool to configure the Telegram Bot setup.
- `type`: "choice"
- `multiSelect`: true
- `question`: "Which features should I include for your Telegram Bot?"
- `header`: "Bot Setup"
- `options`:
  - Label: "Persistence", Description: "Add PicklePersistence or DictPersistence for storing user data."
  - Label: "Webhooks", Description: "Scaffold a webhook setup instead of long polling."
  - Label: "Database", Description: "Add an async database connection (e.g., Tortoise ORM or SQLAlchemy)."
  - Label: "Conversation", Description: "Add a complex ConversationHandler template."

Wait for the response.

2. **File Structure**: Create the following directory structure:
- `bot/`
  - `__init__.py`
  - `main.py` (Bot entry point and handler registration)
  - `handlers/` (Command and message handlers)
  - `utils/` (Helper functions and formatters)
  - `database/` (Optional database logic)
- `requirements.txt` (python-telegram-bot, python-dotenv)
- `.env` (TELEGRAM_TOKEN and other secrets)
- `README.md` (Instructions on how to get a token from @BotFather)

3. **Environment Setup**:
- Create virtual environment `venv`.
- Install dependencies from `requirements.txt`.
- Remind the user to add their bot token to the `.env` file.
"""
