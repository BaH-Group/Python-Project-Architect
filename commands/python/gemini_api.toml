description = "Scaffolds a professional AI project structure using the Google Gemini API, including features like multimodal support and tool integration."
prompt = """
Role: Gemini API Project Architect.

1. **Ask the user**:

Use the `ask_user` tool to confirm the Gemini API project setup.
- `type`: "choice"
- `multiSelect`: true
- `question`: "Which Gemini API features should I include?"
- `header`: "Gemini API Setup"
- `options`:
  - Label: "Basic API Integration", Description: "Set up a basic Gemini API client and example usage."
  - Label: "Streamlit", Description: "Add a Streamlit UI for an interactive chat experience."
  - Label: "Multimodal", Description: "Add support for image/audio input and specialized prompt templates."
  - Label: "LangChain", Description: "Integrate LangChain for building complex AI chains and memory."
  - Label: "Caching", Description: "Add basic caching mechanisms for API responses to save tokens."

Wait for the response.

2. **File Structure**: Create the following directory structure:
- `app/`
  - `__init__.py`
  - `main.py` (Gemini API initialization and logic)
  ```
  import os
  from dotenv import load_dotenv
  from google import genai

  load_dotenv()

  api_key = os.getenv("GOOGLE_API_KEY")

  client = genai.Client(api_key=api_key)
```
  - `utils/` (Helper functions for file processing and prompt engineering)
- Create  `requirements.txt` with following dependencies:
  - python-dotenv
  - google-genai
  - pydantic
  - Options based on user selection
    

- `.env` (Include a placeholder for GOOGLE_API_KEY)
- `README.md` (Instructions on how to get an API key and run the app)

3. **Environment Setup**:
- Create virtual environment `venv`.
- Install dependencies from `requirements.txt`.
- Provide a summary of how to run the app.
"""

